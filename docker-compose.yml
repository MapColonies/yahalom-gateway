version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: yahalom-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Libot4allnonprod
      POSTGRES_DB: log_object
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
